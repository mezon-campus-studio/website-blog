/**
 * Re-export RoleGate (alias for RoleGuard) from the auth feature.
 * Kept here for backward-compatible import path @/components/auth/RoleGate.
 */
export { RoleGuard as default } from '@/features/auth/components/RoleGuard';
