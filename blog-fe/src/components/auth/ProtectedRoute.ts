/**
 * Re-export ProtectedRoute from the auth feature.
 * Kept here for backward-compatible import path @/components/auth/ProtectedRoute.
 */
export { ProtectedRoute as default } from '@/features/auth/components/ProtectedRoute';

